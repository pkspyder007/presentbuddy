# Wallpapers

This directory should contain default professional wallpapers.

For now, the app will use a solid color or user-provided wallpaper.

To add a default wallpaper:
1. Add a professional, neutral wallpaper image (e.g., `default.jpg`)
2. The app will use it when changing wallpapers
